<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <style>
        /* Global Reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html, body {
    height: 100%;
    width: 100%;
    font-family: 'Helvetica Neue', sans-serif;
    background-color: #f4f4f9;
}

/* Main Container */
.container {
    width: 100%;
    max-width: 400px;
    padding: 20px;
    margin: 20px auto;
    background-color: #ffffff;
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
    border-radius: 12px;
}

/* Card Styling */
h1 {
    font-size: 1.8rem;
    color: #333;
    margin-bottom: 20px;
    text-align: center;
}

/* Form Styling */
label {
    display: block;
    margin-bottom: 10px;
    color: #555;
}

button {
    width: 100%;
    padding: 12px;
    background-color: #82c4c3;
    border: none;
    border-radius: 8px;
    color: #fff;
    font-size: 1.1rem;
    cursor: pointer;
    transition: background-color 0.3s ease;
    margin-top: 20px;
}

button:hover {
    background-color: #68a7a6;
}

/* Error Message */
.error-message {
    color: red;
    text-align: center;
    margin-top: 15px;
}

/* Links */
.submit-wish, .view-wishes, .logout-link {
    display: block;
    text-align: center;
    margin-top: 20px;
    color: #333;
    text-decoration: none;
}

.submit-wish:hover, .view-wishes:hover, .logout-link:hover {
    text-decoration: underline;
}

/* Responsive Design */
@media (max-width: 768px) {
    .container {
        padding: 15px;
    }

    h1 {
        font-size: 1.6rem;
    }

    button {
        font-size: 1rem;
        padding: 10px;
    }
}

    </style>
</head>
<body>
    <div class="container">
        <h1>Welcome, <%= user.email || 'Anonymous User' %></h1>
        <form action="/update-preferences" method="POST">
            <label>
                <input type="checkbox" name="anonymity" <%= preferences.anonymity ? 'checked' : '' %>>
                Stay Anonymous
            </label>
            <br>
            <label>
                <input type="checkbox" name="messagingEnabled" <%= preferences.messagingEnabled ? 'checked' : '' %>>
                Enable Messaging
            </label>
            <br>
            <button type="submit">Update Preferences</button>
        </form>
        <% if (locals.error) { %>
            <p class="error-message"><%= error %></p>
        <% } %>
        <a href="/submit-wish" class="submit-wish">Submit a Wish</a>
        <a href="/wishes" class="view-wishes">View All Wishes</a>
        <a href="/logout" class="logout-link">Logout</a>
    </div>
</body>
</html>
